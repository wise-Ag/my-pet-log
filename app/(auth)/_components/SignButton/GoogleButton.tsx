import SignButton from ".";

const GoogleButton = () => {
  const onClick = () => {};

  return (
    <div>
      <SignButton type="google" action="시작하기" onClick={onClick} />
    </div>
  );
};

export default GoogleButton;
